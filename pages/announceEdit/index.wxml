<view class="contact">
    <van-field input-class="contact-input" model:value="{{ content }}" label="公告" type="textarea" placeholder="请输入公告内容" autosize border="{{ false }}" />
    <button bind:tap="onSubmit" class="submit-btn">提交</button>
</view>