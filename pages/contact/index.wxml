<view class="contact">
    <van-field input-class="contact-input" model:value="{{ message }}" label="意见" type="textarea" placeholder="请输入意见" autosize border="{{ false }}" />
    <button bind:tap="onSubmit" class="submit-btn">提交</button>
</view>